@page
@model ActorsMovies.Pages.ActorsInMovies.IndexModel 

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActorsInMovie[0].Movie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActorsInMovie[0].Actors)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Actor[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Movie[0].Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ActorsInMovie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Actors.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Actors.FirstName) @Html.DisplayFor(modelItem => item.Actors.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Title)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
